function greet(name) {
  return "Hello, " + name;
}

const add = function(a, b) {
  return a + b;
};

const multiply = (x, y) => x * y;

console.log(greet("Atreyi"));
console.log(add(2, 3));
console.log(multiply(3, 4));
